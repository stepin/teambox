- content_for :column do
  - if @group.has_logo?
    %img{:src => @group.logo.url(:top), :alt => @group.name, :style => "height:24px"}/
  - else
    %img{:src => "/images/header_logo_black.png", :alt => @group.name, :style => "height:24px"}/
  
  %h2= "Projects"
  #projects
    = render :partial => 'groups/project', :collection => @group.projects
  = add_group_project_link
  = group_project_form

  %h2= "Your Groups"
  = list_groups(current_user.groups)